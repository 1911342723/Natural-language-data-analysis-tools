# 智能数据分析工具 - 前端

基于 React + Vite + Ant Design 构建的现代化数据分析前端应用。

## ✨ 核心功能

### 1. 文件上传与预览
- 📁 支持 Excel (.xlsx, .xls) 和 CSV 文件上传
- 📊 自动解析并展示数据预览（前100行）
- 📈 显示数据统计信息（行数、列数、文件大小）

### 2. 智能字段选择
- 🔍 可搜索的字段列表
- 🏷️ 字段类型标签（int, float, string, datetime等）
- 👁️ 实时预览已选字段数据

### 3. 自然语言对话
- 💬 直观的对话式交互界面
- 📝 示例需求快速填充
- 🔄 对话历史记录

### 4. Agent 执行过程可视化 ⭐
- 🤖 实时展示 Agent 执行步骤
- 📋 步骤流程图（生成代码 → 执行 → 判断 → 修复）
- 💻 Monaco Editor 代码高亮显示
- 📊 执行输出和错误信息展示

### 5. 结果展示
- 📊 表格数据展示（支持排序、筛选、导出CSV）
- 📈 图表展示（支持查看大图、下载）
- 🤖 AI 生成的分析总结（Markdown格式）

### 6. 历史记录
- 🗂️ 侧边栏历史记录列表
- 🔍 搜索历史记录
- 🗑️ 删除不需要的记录

## 🚀 快速开始

### 安装依赖

```bash
npm install
# 或
yarn install
# 或
pnpm install
```

### 启动开发服务器

```bash
npm run dev
```

访问 http://localhost:3000

### 构建生产版本

```bash
npm run build
```

### 预览生产构建

```bash
npm run preview
```

## 📦 技术栈

- **框架**: React 18
- **构建工具**: Vite 5
- **UI 库**: Ant Design 5.21
- **状态管理**: Zustand 4.5
- **代码编辑器**: Monaco Editor 4.6
- **图表库**: Recharts 2.12
- **Markdown 渲染**: react-markdown 9.0
- **HTTP 客户端**: Axios 1.7
- **工具库**: ahooks, dayjs

## 📂 项目结构

```
frontend/
├── src/
│   ├── main.jsx                 # 应用入口
│   ├── App.jsx                  # 根组件
│   ├── index.css                # 全局样式
│   ├── components/              # 组件目录
│   │   ├── Layout/              # 布局组件
│   │   │   ├── MainLayout.jsx
│   │   │   └── MainLayout.css
│   │   ├── FileUpload/          # 文件上传
│   │   │   ├── FileUpload.jsx
│   │   │   └── FileUpload.css
│   │   ├── DataPreview/         # 数据预览
│   │   │   ├── DataPreview.jsx
│   │   │   └── DataPreview.css
│   │   ├── FieldSelector/       # 字段选择
│   │   │   ├── FieldSelector.jsx
│   │   │   └── FieldSelector.css
│   │   ├── WorkArea/            # 工作区
│   │   │   ├── WorkArea.jsx
│   │   │   └── WorkArea.css
│   │   ├── ChatArea/            # 对话区域 ⭐
│   │   │   ├── ChatArea.jsx
│   │   │   ├── ChatArea.css
│   │   │   ├── ConversationList.jsx
│   │   │   ├── ConversationList.css
│   │   │   ├── AgentExecution.jsx    # Agent执行可视化
│   │   │   └── AgentExecution.css
│   │   ├── ResultDisplay/       # 结果展示
│   │   │   ├── ResultDisplay.jsx
│   │   │   ├── TableResult.jsx
│   │   │   ├── ChartResult.jsx
│   │   │   ├── AISummary.jsx
│   │   │   └── *.css
│   │   └── History/             # 历史记录
│   │       ├── HistorySidebar.jsx
│   │       └── HistorySidebar.css
│   ├── services/                # API 服务
│   │   └── api.js
│   └── store/                   # 状态管理
│       └── useAppStore.js
├── index.html
├── vite.config.js
├── package.json
└── README.md
```

## 🔌 API 接口

前端通过以下 API 与后端通信：

### 文件相关
- `POST /api/upload` - 上传文件
  
### Session 相关
- `POST /api/session/create` - 创建 Jupyter Session

### Agent 相关
- `POST /api/agent/analyze` - 提交分析请求
- `GET /api/agent/status/:taskId` - 获取执行状态
- `POST /api/agent/stop/:taskId` - 停止执行

### 历史记录
- `GET /api/history/list` - 获取历史记录列表
- `GET /api/history/:id` - 获取详情
- `DELETE /api/history/:id` - 删除记录

### 导出
- `POST /api/export` - 导出结果（md/pdf/docx）

## 🎨 核心设计理念

### 1. Agent 执行过程可视化
借鉴 Jupyter Notebook 的交互模式：
- 每次代码执行创建一个"单元格"
- 实时显示执行输出（stdout/stderr）
- 错误时自动重试，展示修复过程
- 步骤流程图清晰展示执行进度

### 2. 状态管理
使用 Zustand 轻量级状态管理：
- 文件数据状态
- 字段选择状态
- Session 状态
- Agent 执行状态
- 对话历史状态

### 3. 响应式设计
- 侧边栏可折叠
- 表格虚拟滚动
- 图表自适应

## 🔧 配置说明

### 代理配置
`vite.config.js` 中已配置开发服务器代理：

```javascript
server: {
  port: 3000,
  proxy: {
    '/api': {
      target: 'http://localhost:8000',
      changeOrigin: true,
    },
  },
}
```

### 环境变量
可以创建 `.env` 文件配置环境变量：

```env
VITE_API_BASE_URL=http://localhost:8000
```

## 🐛 调试技巧

### 查看 Agent 执行日志
打开浏览器控制台，可以看到：
- `✅ Session 创建成功`
- `🤖 Agent 任务启动`
- `📊 Agent 状态`

### 网络请求
在 Network 面板可以查看：
- 文件上传进度
- API 请求响应
- 轮询状态更新

## 📝 待优化项

- [ ] WebSocket 实时推送（替代轮询）
- [ ] 代码编辑功能（允许用户修改生成的代码）
- [ ] 更多图表类型支持
- [ ] 暗黑模式
- [ ] 国际化支持

## 📄 License

MIT

---

**注意**：此前端需要配合后端 Python FastAPI 服务使用。


